<body data-bs-theme="light">
  <!-- Navbar -->
  <header class="navbar sticky-top navbar-expand-lg glass-nav shadow-sm border-bottom w-100">
    <div class="container-fluid d-flex justify-content-between align-items-center px-4">


<div class="d-flex align-items-center">
  <%= link_to root_path, class: "navbar-brand d-flex align-items-center logo-hover text-decoration-none", data: { turbo: false } do %>
    <%= image_tag(
          "https://res.cloudinary.com/dirgjwkty/image/upload/v1749569642/Transparent_Logo_d8cyxa.png",
          alt: "StudySpark Logo",
          style: "height: 74px; margin-right: 0.35rem; object-fit: contain;",
          class: "logo-img"
        ) %>
    <span class="logo-text fw-semibold fs-4 mb-0 align-middle">StudySpark</span>
  <% end %>
</div>









     <!-- Center: Features & Login -->
<div class="d-flex align-items-center">
  <%= link_to "Features", "#features", class: "btn btn-outline-warning fw-semibold mb-0 me-4" %>
  <%= link_to "Login", "/users/sign_in", class: "btn btn-outline-warning fw-semibold mb-0" %>
</div>


      <!-- Right: Get Started -->
      <div class="d-flex align-items-center">
        <%= link_to "Get Started", "/users/sign_up", class: "btn btn-warning text-white fw-semibold mb-0" %>
      </div>

    </div>
  </header>

  <!-- Hero -->
  <section class="hero-section fade-in-section d-flex align-items-center justify-content-center text-center" style="padding-top: 100px;">
    <div class="container mt-5">
      <h1 class="display-4 fw-bold">Learn Smart, Achieve More</h1>
      <p class="hero-tagline fs-5 mt-3">
        <%=meta_description%>
      </p>
      <div class="hero-cta mt-5">
        <%= link_to "Start Learning Today", "/users/sign_up", class: "btn btn-lg btn-warning text-white fw-semibold" %>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section class="features fade-in-section py-5 bg-white" id="features">
    <div class="container">
      <h2 class="section-title text-center mb-5 text-dark">Designed for Everyone</h2>
      <div class="row g-4 justify-content-center">

        <!-- Learners -->
        <div class="col-md-6 col-lg-4">
          <div class="glass-card p-4 h-100 text-start">
            <h3 class="fw-semibold">🎓 For Learners</h3>
            <ul class="feature-list mt-2">
              <li>AI-Assisted Tutoring Sessions</li>
              <li>Adaptive Learning Paths</li>
              <li>On-Demand Tutor Access</li>
              <li>Structured Practice Materials</li>
              <li>Clear Progress Tracking</li>
            </ul>
          </div>
        </div>

        <!-- Tutors -->
        <div class="col-md-6 col-lg-4">
          <div class="glass-card p-4 h-100 text-start">
            <h3 class="fw-semibold">👨‍🏫 For Tutors</h3>
            <ul class="feature-list mt-2">
              <li>Automated Lesson Briefs</li>
              <li>Data-Driven Student Matching</li>
              <li>Flexible Scheduling Tools</li>
              <li>Smart Resource Preparation</li>
              <li>Performance Analytics</li>
            </ul>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Problem & Solution -->
  <section id="problem-solution" class="fade-in-section py-5">
    <div class="container">
      <div class="row g-5">

        <!-- Problems -->
        <div class="col-md-6">
          <h2 class="section-title title-problems mb-4">The Problems We Solve</h2>
          <div class="d-grid gap-3">
            <div class="glass-card p-4 text-start">
              <h4 class="fw-semibold">For Learners</h4>
              <p>Difficulty accessing quality help, unstructured practice, and opaque progress tracking.</p>
            </div>
            <div class="glass-card p-4 text-start">
              <h4 class="fw-semibold">For Tutors</h4>
              <p>Time-consuming prep work, poor student-tutor matches, and scheduling struggles.</p>
            </div>
          </div>
        </div>

        <!-- Solutions -->
        <div class="col-md-6">
          <h2 class="section-title title-solutions mb-4">Our AI-Enhanced Solutions</h2>
          <div class="d-grid gap-3">
            <div class="glass-card p-4 text-start">
              <h4 class="fw-semibold">Scalable Learning Support</h4>
              <p>AI-powered assistance ensures every student gets the help they need, when they need it.</p>
            </div>
            <div class="glass-card p-4 text-start">
              <h4 class="fw-semibold">Intelligent Matching</h4>
              <p>Data-driven algorithms pair students with the perfect tutors based on learning style and needs.</p>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="final-cta fade-in-section text-center py-5" style="background: linear-gradient(135deg, #E7DFF2 0%, #EDF4FA 100%);">
    <div class="container">
      <h2 class="fw-bold mb-3">Ready to Transform Learning?</h2>
      <p class="mb-4 text-dark">Join thousands of students, tutors, and parents who are already achieving more with StudySpark.</p>
      <%= link_to "Get Started Free", "/users/sign_up", class: "btn btn-lg btn-warning text-white fw-semibold" %>
    </div>
  </section>

  <!-- Footer -->
  <footer class="site-footer text-center py-4 bg-white border-top">
    <div class="container">
      <p>&copy; <%= Time.current.year %> StudySpark. All rights reserved.</p>
      <ul class="list-inline mt-2">
        <li class="list-inline-item"><a href="#" class="text-muted">Privacy Policy</a></li>
        <li class="list-inline-item"><a href="#" class="text-muted">Terms of Service</a></li>
        <li class="list-inline-item"><a href="#features" class="text-muted">Features</a></li>
      </ul>
    </div>
  </footer>

  <!-- JS: Scroll Animations -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('.fade-in-section').forEach(section => {
        new IntersectionObserver(entries => {
          entries.forEach(e => e.isIntersecting && e.target.classList.add('in-view'));
        }, { threshold: 0.1 }).observe(section);
      });

      const nav = document.querySelector('.glass-nav');
      let lastY = window.pageYOffset;
      window.addEventListener('scroll', () => {
        const currentY = window.pageYOffset;
        if (currentY > lastY && currentY > 100) {
          nav.classList.add('scroll-down');
          nav.classList.remove('scroll-up');
        } else {
          nav.classList.add('scroll-up');
          nav.classList.remove('scroll-down');
        }
        lastY = currentY;
      });
    });
  </script>
</body>
